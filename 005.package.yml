- name: Demo on Package
  hosts: all
  tasks:
    - name: UNInstalling nginx
      ansible.builtin.package: 
        name: nginx
        state: absent

    - name: UNInstalling nginx
      ansible.builtin.package: 
        name: nginx
        state: present